extends ./layout/layout

block content
  button.start-quiz-btn Start quiz
  table.mui-table.mui-table--bordered.participants-list(data-count=allTasks.length)
    thead
      tr
        th Participant
        each task in allTasks
          th= task.name
        th Total time
    tbody
